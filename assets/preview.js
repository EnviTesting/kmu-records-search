(() => {
  'use strict';
  const DATASETS=['documents','judgments','press_releases','external_references','parliamentary_evidence','gazette_records','statistical_context','regional_references','international_references','videos'];
  const TT_MAX_BOUNDS=[[9.80,-62.18],[11.58,-60.28]];
  const IMA_LIBRARY='https://www.ima.gov.tt/library/';
  const EMA_REQUEST='https://www.ema.co.tt/information-centre-general-request/';
  const state={records:[],media:[],places:[],observances:[],aaqmn:[],spatialLayers:[],boundaries:null,source:'all',topic:'all',viewMode:'both',showMedia:false,showAAQMN:false,imaLayer:'off',calendarTopic:'all',map:null,markerLayer:null,aaqmnLayer:null,imaFeatureLayer:null,boundaryLayer:null,tileLayer:null,tileErrors:0,mode:'pending',imaLoadToken:0};
  const $=id=>document.getElementById(id);
  const clean=v=>String(v??'').replace(/\s+/g,' ').trim();
  const lower=v=>clean(v).toLowerCase();
  const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const uniq=rows=>[...new Map(rows.map(r=>[r.id,r])).values()];
  document.addEventListener('DOMContentLoaded',init);

  async function init(){
    bindTabs();
    const [records,media,places,obs,boundaries,aaqmn,layers]=await Promise.all([
      loadRecords(),loadJson('data/ema_in_news.json'),loadJson('data/spatial_preview.json'),loadJson('data/environmental_observances.json'),loadJson('data/tt_adm1_fallback.geojson'),loadJson('data/aaqmn_stations.json'),loadJson('data/spatial_layers.json')
    ]);
    state.records=records; state.media=Array.isArray(media)?media:[]; state.places=Array.isArray(places)?places:[]; state.observances=Array.isArray(obs)?obs:[];
    state.aaqmn=Array.isArray(aaqmn)?aaqmn:[]; state.spatialLayers=Array.isArray(layers)?layers:[]; state.boundaries=boundaries&&boundaries.type==='FeatureCollection'?boundaries:null;
    populateFilters(); bindFilters(); renderCalendar(); initMap();
  }

  async function loadJson(path){try{const r=await fetch(path,{cache:'no-store'});if(r.ok)return await r.json();}catch{}return null;}
  async function loadRecords(){const chunks=await Promise.all(DATASETS.map(async db=>((await loadJson(`data/${db}.json`))||[]).map(r=>normalise(r,db))));return chunks.flat();}
  function normalise(r,db){
    const allText=[r.title,r.short_title,r.description,r.summary_snippet,r.knowledge_area,r.programme_area,r.theme,r.record_type_simplified,r.record_category,r.issuer,r.source_agency,...(Array.isArray(r.keywords)?r.keywords:[])].join(' ');
    const geoText=[r.title,r.short_title,r.description,r.summary_snippet,r.knowledge_area,r.programme_area,r.theme,...(Array.isArray(r.keywords)?r.keywords:[])].join(' ').replace(/trinidad\s*(?:and|&)\s*tobago/gi,' ').replace(/\bt\s*&\s*t\b/gi,' ');
    return {...r,database:db,text:lower(allText),geoText:lower(geoText)};
  }
  function sourceGroup(r){if(r.database==='external_references')return'government';if(r.database==='parliamentary_evidence')return'parliament';if(r.database==='gazette_records')return'gazette';if(r.database==='statistical_context')return'statistical';if(r.database==='regional_references')return'regional';if(r.database==='international_references')return'international';if(r.database==='videos')return'video';if(r.database==='judgments')return'court';return'ema';}
  function sourceLabel(r){return {ema:'EMA',government:'Government',parliament:'Parliament',gazette:'Gazette',statistical:'Research & statistics',regional:'Regional',international:'International · T&T',video:'Video',court:'Court / tribunal'}[sourceGroup(r)]||'Source';}
  function baseRecordFilter(r){return state.source==='all'||sourceGroup(r)===state.source;}

  function bindTabs(){$('mapTab').addEventListener('click',()=>setTab('map'));$('calendarTab').addEventListener('click',()=>setTab('calendar'));}
  function setTab(which){const map=which==='map';$('mapTab').classList.toggle('active',map);$('calendarTab').classList.toggle('active',!map);$('mapTab').setAttribute('aria-selected',String(map));$('calendarTab').setAttribute('aria-selected',String(!map));$('mapPanel').classList.toggle('hidden',!map);$('calendarPanel').classList.toggle('hidden',map);if(map&&state.map)setTimeout(()=>state.map.invalidateSize(),0);}
  function populateFilters(){const topics=[...new Set(state.places.map(p=>p.topic).filter(Boolean))].sort();$('mapTopicFilter').innerHTML='<option value="all">All preview topics</option>'+topics.map(t=>`<option>${esc(t)}</option>`).join('');const ct=[...new Set(state.observances.map(o=>o.topic).filter(Boolean))].sort();$('calendarTopicFilter').innerHTML='<option value="all">All topics</option>'+ct.map(t=>`<option>${esc(t)}</option>`).join('');}
  function bindFilters(){
    $('mapSourceFilter').addEventListener('change',e=>{state.source=e.target.value;renderMap();});
    $('mapTopicFilter').addEventListener('change',e=>{state.topic=e.target.value;renderMap();});
    $('mapViewMode').addEventListener('change',e=>{state.viewMode=e.target.value;renderMap();});
    $('mediaMapToggle')?.addEventListener('change',e=>{state.showMedia=Boolean(e.target.checked);renderMap();});
    $('aaqmnMapToggle')?.addEventListener('change',e=>{state.showAAQMN=Boolean(e.target.checked);renderMap();});
    $('imaLayerSelect')?.addEventListener('change',e=>{state.imaLayer=e.target.value;renderMap();});
    $('resetMapFilters').addEventListener('click',()=>{state.source='all';state.topic='all';state.viewMode='both';state.showMedia=false;state.showAAQMN=false;state.imaLayer='off';$('mapSourceFilter').value='all';$('mapTopicFilter').value='all';$('mapViewMode').value='both';if($('mediaMapToggle'))$('mediaMapToggle').checked=false;if($('aaqmnMapToggle'))$('aaqmnMapToggle').checked=false;if($('imaLayerSelect'))$('imaLayerSelect').value='off';renderMap();});
    $('calendarTopicFilter').addEventListener('change',e=>{state.calendarTopic=e.target.value;renderCalendar();});
  }
  function topicTerms(){if(state.topic==='all')return[];return [...new Set(state.places.filter(p=>p.topic===state.topic).flatMap(p=>p.match_terms||[]).map(lower).filter(Boolean))];}
  function topicRecordFilter(r){const terms=topicTerms();return !terms.length||terms.some(t=>r.text.includes(t));}
  function matches(place){const terms=(place.match_terms||[]).map(lower);return state.records.filter(r=>baseRecordFilter(r)&&terms.some(t=>t&&r.text.includes(t)));}
  function mediaMatches(place){if(!state.showMedia)return[];const terms=[lower(place.name),...(place.match_terms||[]).map(lower)].filter(Boolean);return state.media.filter(r=>{const gs=[...(r.geography||[]),...(r.explicit_places||[])].map(lower);return gs.some(g=>terms.some(t=>g===t||g.includes(t)||t.includes(g)));});}
  function networkAirRecords(){return state.records.filter(r=>baseRecordFilter(r)&&(/ambient air quality|aaqmn|air quality monitoring/.test(r.text)||lower(r.knowledge_area)==='air & atmosphere'));}
  function stationMatches(station){const aliases=[station.name,station.site_name,...(station.aliases||[])].map(lower).filter(Boolean);const specific=state.records.filter(r=>baseRecordFilter(r)&&aliases.some(a=>a.length>3&&r.text.includes(a)));return uniq([...specific,...networkAirRecords()]);}
  function layerRelatedRecords(cfg,props={}){const propTerms=Object.values(props||{}).filter(v=>typeof v==='string'&&v.length>3&&v.length<90).slice(0,8);const terms=[...(cfg.match_terms||[]),...propTerms].map(lower).filter(Boolean);return state.records.filter(r=>baseRecordFilter(r)&&terms.some(t=>r.text.includes(t))).slice(0,40);}

  function regionAliases(name){const n=lower(name);const map={'couva-tabaquite-talparo':['couva-tabaquite-talparo','couva','tabaquite','talparo'],'mayaro/rio claro':['mayaro/rio claro','mayaro rio claro','mayaro','rio claro'],'penal-debe':['penal-debe','penal','debe'],'san juan-laventille':['san juan-laventille','san juan','laventille'],'tunapuna/piarco':['tunapuna/piarco','tunapuna piarco','tunapuna','piarco']};return map[n]||[n];}
  function matchesRegion(name){const aliases=regionAliases(name);return state.records.filter(r=>baseRecordFilter(r)&&topicRecordFilter(r)&&aliases.some(a=>a&&r.geoText.includes(a)));}
  function regionMatchMap(){const m=new Map();for(const f of state.boundaries?.features||[]){const n=f?.properties?.NAME_1;if(n)m.set(n,matchesRegion(n));}return m;}

  function initMap(){if(window.L&&typeof window.L.map==='function'){try{initLeaflet();return;}catch(err){console.warn(err);setStatus('Interactive basemap could not be started. Using the local Trinidad and Tobago boundary fallback.','warn');}}else setStatus('Leaflet could not be loaded. Using the local Trinidad and Tobago boundary fallback.','warn');initFallback();}
  function initLeaflet(){
    state.mode='leaflet';$('leafletMap').classList.remove('hidden');$('mapFallback').classList.add('hidden');
    state.map=L.map('leafletMap',{zoomControl:true,minZoom:8,maxZoom:15,maxBounds:TT_MAX_BOUNDS,maxBoundsViscosity:1.0,worldCopyJump:false,preferCanvas:true});state.map.attributionControl.setPrefix(false);
    state.tileLayer=L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; <a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener">OpenStreetMap</a> contributors'}).addTo(state.map);
    state.tileLayer.on('tileload',()=>{if(state.tileErrors===0)setStatus('OpenStreetMap basemap active. Optional layers remain off until selected.','ok');});
    state.tileLayer.on('tileerror',()=>{state.tileErrors+=1;if(state.tileErrors===1)setStatus('Some OpenStreetMap tiles are unavailable. The local Trinidad and Tobago boundary layer remains available.','warn');if(state.tileErrors>=6){try{state.map.removeLayer(state.tileLayer);}catch{}setStatus('OpenStreetMap basemap unavailable. Showing the locally stored Trinidad and Tobago administrative boundary layer instead.','warn');}});
    if(state.boundaries){state.boundaryLayer=L.geoJSON(state.boundaries,{style:()=>boundaryStyle(0),onEachFeature:(feature,layer)=>{const n=feature?.properties?.NAME_1;if(!n)return;layer.on('click',()=>{if(state.viewMode!=='places')showRegion(n,matchesRegion(n));});layer.on('mouseover',e=>{if(state.viewMode!=='places')e.target.setStyle({weight:2.3});});layer.on('mouseout',e=>{if(state.boundaryLayer)state.boundaryLayer.resetStyle(e.target);});}}).addTo(state.map);const b=state.boundaryLayer.getBounds();if(b.isValid())state.map.fitBounds(b.pad(.04));else state.map.fitBounds([[10.0,-61.95],[11.4,-60.45]]);}else state.map.fitBounds([[10.0,-61.95],[11.4,-60.45]]);
    state.markerLayer=L.layerGroup().addTo(state.map);state.aaqmnLayer=L.layerGroup().addTo(state.map);renderMap();
  }
  function boundaryStyle(count){if(state.viewMode==='places')return{color:'#6f8d85',weight:1,fillColor:'#dfeae5',fillOpacity:.06,interactive:false};const opacity=count?Math.min(.62,.14+Math.log2(count+1)*.10):.06;return{color:'#557b70',weight:1.2,fillColor:'#087d6f',fillOpacity:opacity,interactive:true};}
  function updateLeafletBoundaries(regionMap){if(!state.boundaryLayer)return;state.boundaryLayer.eachLayer(layer=>{const n=layer.feature?.properties?.NAME_1||'';const rows=regionMap.get(n)||[];layer.setStyle(boundaryStyle(rows.length));if(state.viewMode==='places')layer.unbindTooltip();else layer.bindTooltip(`${n} · ${rows.length} explicitly matched record${rows.length===1?'':'s'}`,{sticky:true});});}

  function renderMap(){
    const shown=state.places.filter(p=>state.topic==='all'||p.topic===state.topic);const placeMap=new Map();const regionMap=regionMatchMap();const unique=new Set();
    if(state.viewMode!=='regions')shown.forEach(p=>{const m=matches(p);placeMap.set(p.id,m);m.forEach(r=>unique.add(r.id));});if(state.viewMode!=='places')for(const rows of regionMap.values())rows.forEach(r=>unique.add(r.id));$('mappedRecordCount').textContent=unique.size;
    if(state.mode==='leaflet'){renderLeafletMarkers(state.viewMode==='regions'?[]:shown,placeMap);renderAAQMN();updateLeafletBoundaries(regionMap);renderIMALayer();}else renderFallback(state.viewMode==='regions'?[]:shown,placeMap,regionMap);
    if(!shown.length&&state.viewMode!=='regions')$('placePanel').innerHTML='<p class="empty-state">No preview places match the selected topic.</p>';
  }
  function renderLeafletMarkers(shown,matchMap){
    if(!state.markerLayer)return;state.markerLayer.clearLayers();
    for(const p of shown){const rows=matchMap.get(p.id)||[];const mc=mediaMatches(p).length;if(state.showMedia&&mc){const mr=Math.min(30,9+Math.sqrt(mc)*2.6);const ring=L.circleMarker([p.lat,p.lon],{radius:mr,weight:3,color:'#c47a13',fillColor:'#f0aa3b',fillOpacity:.10});ring.bindTooltip(`${p.name} · ${mc} media article${mc===1?'':'s'}`,{direction:'top'});ring.on('click',()=>showPlace(p.id,rows));ring.addTo(state.markerLayer);}const radius=Math.min(19,7+Math.sqrt(rows.length)*2.1);const marker=L.circleMarker([p.lat,p.lon],{radius,weight:2,color:'#005c52',fillColor:'#006b5f',fillOpacity:.88});marker.bindTooltip(`${p.name} · ${rows.length} knowledge record${rows.length===1?'':'s'}${state.showMedia&&mc?` · ${mc} media`:''}`,{direction:'top'});marker.on('click',()=>showPlace(p.id,rows));marker.addTo(state.markerLayer);}
  }
  function renderAAQMN(){if(!state.aaqmnLayer)return;state.aaqmnLayer.clearLayers();if(!state.showAAQMN)return;for(const s of state.aaqmn){const marker=L.circleMarker([s.lat,s.lon],{radius:7,weight:2,color:'#245e8b',fillColor:s.needs_verification?'#dbe9f5':'#4f90c2',fillOpacity:.95});const qualifier=s.needs_verification?'coordinate requires verification':'EMA-published GPS conversion';marker.bindTooltip(`${s.name} · AAQMN · ${qualifier}`,{direction:'top'});marker.on('click',()=>showStation(s));marker.addTo(state.aaqmnLayer);}}

  function drawIMAGeo(cfg,geo,sourceLabel){
    state.imaFeatureLayer=L.geoJSON(geo,{style:()=>({color:'#6b4c8a',weight:2,fillColor:'#a88cc4',fillOpacity:.18}),pointToLayer:(feature,latlng)=>L.circleMarker(latlng,{radius:6,weight:2,color:'#6b4c8a',fillColor:'#a88cc4',fillOpacity:.85}),onEachFeature:(feature,layer)=>{const label=featureLabel(feature?.properties);layer.bindTooltip(`${cfg.name}${label?` · ${label}`:''}`,{sticky:true});layer.on('click',()=>showIMAFeature(cfg,feature?.properties||{}));}}).addTo(state.map);
    const b=state.imaFeatureLayer.getBounds?.();if(b&&b.isValid())state.map.fitBounds(b.pad(.08));setStatus(`${cfg.name} active (${sourceLabel}). IMA layers are optional and only one is displayed at a time.`,'ok');showIMALayerSummary(cfg,geo.features?.length||0);
  }
  async function renderIMALayer(){
    if(state.imaFeatureLayer&&state.map){try{state.map.removeLayer(state.imaFeatureLayer);}catch{}state.imaFeatureLayer=null;}const token=++state.imaLoadToken;if(state.imaLayer==='off')return;
    const cfg=state.spatialLayers.find(x=>x.id===state.imaLayer);if(!cfg||!cfg.service_url||state.mode!=='leaflet')return;
    setStatus(`Loading ${cfg.name} from the IMA Marine Data Hub…`,'');let liveError=null;
    try{
      const service=cfg.service_url.replace(/\/$/,'');let meta=await fetchJson(`${service}?f=pjson`);let layers=Array.isArray(meta?.layers)?meta.layers:[];if(!layers.length){const lm=await fetchJson(`${service}/layers?f=pjson`);layers=Array.isArray(lm?.layers)?lm.layers:[];}if(!layers.length)throw new Error('No queryable layer was advertised by the service.');
      const layerId=layers[0].id;const q=`${service}/${layerId}/query?where=1%3D1&outFields=*&returnGeometry=true&outSR=4326&f=geojson`;const geo=await fetchJson(q);if(token!==state.imaLoadToken)return;if(!geo||geo.type!=='FeatureCollection')throw new Error('The service did not return GeoJSON.');drawIMAGeo(cfg,geo,'live IMA Marine Data Hub');return;
    }catch(err){liveError=err;console.warn('IMA live layer failed',err);}
    if(cfg.cache_url){try{const geo=await fetchJson(cfg.cache_url);if(token!==state.imaLoadToken)return;if(!geo||geo.type!=='FeatureCollection')throw new Error('Cached file is not a FeatureCollection.');drawIMAGeo(cfg,geo,'cached IMA snapshot');setStatus(`${cfg.name} active from the most recent cached IMA snapshot because the live service was unavailable.`,'warn');return;}catch(cacheErr){console.warn('IMA cache failed',cacheErr);}}
    if(token!==state.imaLoadToken)return;setStatus(`IMA layer unavailable from the live service and no usable cache is present. TRACE has left the layer off; the knowledge records remain searchable. (${liveError?.message||'service unavailable'})`,'warn');$('placePanel').innerHTML=`<h3>${esc(cfg.name)}</h3><p class="empty-state">The IMA Marine Data Hub layer could not be loaded in this browser and no cached snapshot is available. This does not remove the related IMA records from TRACE.</p><p><a href="${esc(cfg.service_url)}" target="_blank" rel="noopener">Open IMA REST service ↗</a></p>`;
  }
  async function fetchJson(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);return await r.json();}
  function featureLabel(props={}){for(const k of ['Name','NAME','name','Site','SITE','Station','STATION','Block','BLOCK','Label','LABEL']){if(clean(props[k]))return clean(props[k]);}return'';}

  function initFallback(){state.mode='fallback';$('leafletMap').classList.add('hidden');$('mapFallback').classList.remove('hidden');renderMap();}
  function fallbackBounds(){if(state.boundaries){const coords=[];for(const f of state.boundaries.features||[])collectCoords(f.geometry?.coordinates,coords);if(coords.length)return{minLon:Math.min(...coords.map(c=>c[0])),maxLon:Math.max(...coords.map(c=>c[0])),minLat:Math.min(...coords.map(c=>c[1])),maxLat:Math.max(...coords.map(c=>c[1]))};}return{minLon:-61.95,maxLon:-60.45,minLat:10.0,maxLat:11.4};}
  function collectCoords(node,out){if(!Array.isArray(node))return;if(typeof node[0]==='number'&&typeof node[1]==='number'){out.push(node);return;}node.forEach(n=>collectCoords(n,out));}
  function proj(lon,lat,b){const pad=30,w=900,h=590;const x=pad+(lon-b.minLon)/(b.maxLon-b.minLon)*(w-pad*2);const y=h-pad-(lat-b.minLat)/(b.maxLat-b.minLat)*(h-pad*2);return[x,y];}
  function geometryPaths(geom,b){if(!geom)return'';const polys=geom.type==='Polygon'?[geom.coordinates]:geom.type==='MultiPolygon'?geom.coordinates:[];return polys.map(poly=>poly.map(ring=>ring.map((c,i)=>{const[x,y]=proj(c[0],c[1],b);return`${i?'L':'M'}${x.toFixed(1)},${y.toFixed(1)}`;}).join(' ')+' Z').join(' ')).join(' ');}
  function fallbackOpacity(count){return state.viewMode==='places'?0.06:(count?Math.min(.62,.14+Math.log2(count+1)*.10):.06);}
  function renderFallback(shown,placeMap,regionMap){
    const host=$('mapFallback');if(!host)return;const b=fallbackBounds();const outlines=state.boundaries?(state.boundaries.features||[]).map(f=>{const n=f.properties?.NAME_1||'Administrative area';const rows=regionMap.get(n)||[];const clickable=state.viewMode!=='places';return`<path class="fallback-area${clickable?' clickable':''}" data-region="${esc(n)}" tabindex="${clickable?'0':'-1'}" d="${geometryPaths(f.geometry,b)}" style="fill-opacity:${fallbackOpacity(rows.length)}"><title>${esc(n)} · ${rows.length} explicitly matched record${rows.length===1?'':'s'}</title></path>`;}).join(''):'<rect class="fallback-area" x="30" y="30" width="840" height="530" />';
    const markers=shown.map(p=>{const rows=placeMap.get(p.id)||[];const[x,y]=proj(p.lon,p.lat,b);const rad=Math.min(20,8+Math.sqrt(rows.length)*2);const mc=mediaMatches(p).length;const mediaRing=state.showMedia&&mc?`<circle class="fallback-media-ring" cx="${x}" cy="${y}" r="${Math.min(30,10+Math.sqrt(mc)*2.6)}"/>`:'';return`<g class="fallback-marker" role="button" tabindex="0" data-place="${esc(p.id)}">${mediaRing}<circle cx="${x}" cy="${y}" r="${rad}"/><text x="${x}" y="${y}">${rows.length}</text><title>${esc(p.name)} · ${rows.length} knowledge record${rows.length===1?'':'s'}${state.showMedia&&mc?` · ${mc} media`:''}</title></g>`;}).join('');
    const air=state.showAAQMN?state.aaqmn.map(s=>{const[x,y]=proj(s.lon,s.lat,b);return`<g class="fallback-air" role="button" tabindex="0" data-station="${esc(s.id)}"><circle cx="${x}" cy="${y}" r="6"/><title>${esc(s.name)} · AAQMN</title></g>`;}).join(''):'';
    host.innerHTML=`<svg viewBox="0 0 900 590" aria-label="Local Trinidad and Tobago boundary fallback"><rect class="fallback-sea" width="900" height="590"/>${outlines}${markers}${air}</svg>`;
    host.querySelectorAll('.fallback-marker').forEach(el=>{const open=()=>showPlace(el.dataset.place,placeMap.get(el.dataset.place)||[]);el.addEventListener('click',open);el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open();}});});host.querySelectorAll('.fallback-air').forEach(el=>{const open=()=>{const s=state.aaqmn.find(x=>x.id===el.dataset.station);if(s)showStation(s);};el.addEventListener('click',open);el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open();}});});host.querySelectorAll('.fallback-area.clickable').forEach(el=>{const open=()=>showRegion(el.dataset.region,regionMap.get(el.dataset.region)||[]);el.addEventListener('click',open);el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open();}});});if(state.imaLayer!=='off')setStatus('IMA live REST layers require Leaflet/browser network access and are not drawn in the local SVG fallback.','warn');
  }

  function setStatus(text,kind){const el=$('mapStatus');if(!el)return;el.textContent=text;el.className=`map-status ${kind||''}`;}
  function recordUrl(r){return r.direct_url||r.source_url||'';}
  function sourceChips(rows){const by={};rows.forEach(r=>by[sourceGroup(r)]=(by[sourceGroup(r)]||0)+1);return Object.entries(by).sort((a,b)=>b[1]-a[1]).map(([k,n])=>`<span class="badge">${esc(({ema:'EMA',government:'Government',parliament:'Parliament',gazette:'Gazette',statistical:'Research/statistics',regional:'Regional',international:'International',video:'Video',court:'Court'}[k]||k))} ${n}</span>`).join('');}
  function accessStatus(r){if(['open_online','request_ema','request_ima','reference_only','link_review'].includes(r.access_status))return r.access_status;const t=lower([r.source_status,r.access_route,r.availability_note,r.notes].join(' '));if(recordUrl(r))return'open_online';if(r.request_url&&/ima\.gov\.tt/.test(r.request_url))return'request_ima';if(/request|held by ema/.test(t))return'request_ema';if(/review|broken/.test(t))return'link_review';return'reference_only';}
  function recordList(rows){return rows.slice(0,12).map(r=>{const u=recordUrl(r),st=accessStatus(r),title=r.short_title||r.title;let action='';if(u)action=`<a href="${esc(u)}" target="_blank" rel="noopener">Open source ↗</a>`;else if(st==='request_ima')action=`<a href="${esc(r.request_url||IMA_LIBRARY)}" target="_blank" rel="noopener">Request from the IMA ↗</a>`;else if(st==='request_ema')action=`<a href="${EMA_REQUEST}" target="_blank" rel="noopener">Request from Information Centre ↗</a>`;else if(st==='link_review')action='<span class="badge">Link under review</span>';else action='<span class="badge">Reference source</span>';return`<article class="mini-record"><h4>${esc(title)}</h4><p>${esc(sourceLabel(r))} · ${esc(r.source_agency||r.issuer||r.record_set||r.database)} · ${esc(r.year||String(r.date_published||'').slice(0,4)||'Year n/a')}</p><div class="record-mini-actions">${action}<button type="button" class="media-badge" data-map-list="${esc(r.id)}">+ My List</button></div></article>`;}).join('');}
  function mediaList(rows){return rows.slice(0,10).map(r=>{const u=r.link_kind==='article'?(r.direct_url||r.source_url||''):'';return`<div class="media-mini-record"><strong>${esc(r.headline||r.title)}</strong><small>${esc(r.outlet||r.source_agency||'Media')} · ${esc(r.publication_date||r.year||'')}</small>${u?`<a href="${esc(u)}" target="_blank" rel="noopener">Open article ↗</a>`:'<span class="media-badge">Article link unavailable</span>'}<button type="button" class="media-badge" data-media-list="${esc(r.id)}">+ My List</button></div>`;}).join('');}
  function bindListButtons(){document.querySelectorAll('[data-map-list]').forEach(b=>b.onclick=()=>{const r=state.records.find(x=>x.id===b.dataset.mapList);if(r&&window.EMAList)window.EMAList.add(r)});document.querySelectorAll('[data-media-list]').forEach(b=>b.onclick=()=>{const r=state.media.find(x=>x.id===b.dataset.mediaList);if(r&&window.EMAList)window.EMAList.add(r)});}
  function commonPanel(title,subtitle,note,rows,searchTerm,mediaRows=[]){$('placePanel').innerHTML=`<h3>${esc(title)}</h3><p class="empty-state">${esc(subtitle)}</p><div class="place-meta"><span class="badge">${rows.length} knowledge record${rows.length===1?'':'s'}</span>${sourceChips(rows)}${state.showMedia?`<span class="badge media-badge-solid">${mediaRows.length} media article${mediaRows.length===1?'':'s'}</span>`:''}</div><p class="empty-state">${esc(note)}</p><div class="legal-map-note">Location and record associations support information discovery only. Administrative-area matching is based on explicit indexed place terms; it does not indicate project boundaries, regulatory jurisdiction, legal effect, environmental condition, compliance status or an EMA determination.</div><div class="record-list">${recordList(rows)||'<p class="empty-state">No knowledge records match the current filters.</p>'}</div>${rows.length>12?`<p class="empty-state">Showing 12 of ${rows.length} matched knowledge records.</p>`:''}${state.showMedia?`<section class="media-subsection"><h4>Media coverage (optional layer)</h4>${mediaList(mediaRows)||'<p class="empty-state">No media archive records are explicitly indexed to this discovery place.</p>'}</section>`:''}<p><a href="index.html?search=${encodeURIComponent(searchTerm)}&related=1">Search “${esc(searchTerm)}” in the main tool →</a></p>`;bindListButtons();}
  function showPlace(id,rows){const p=state.places.find(x=>x.id===id);if(!p)return;commonPanel(p.name,`${p.topic} · ${p.precision} discovery location`,p.note,rows,p.name,mediaMatches(p));}
  function showRegion(name,rows){commonPanel(name,'Administrative-area information coverage','Records shown here explicitly name or are indexed to this administrative area. National records are not assigned to an area simply because they concern Trinidad and Tobago.',rows,name,[]);}
  function showStation(s){const rows=stationMatches(s);const precision=s.needs_verification?'Host/locality coordinate — verify before analytical use':'EMA-published GPS converted to WGS84';const note=`${s.site_name}. ${precision}. ${s.notes||''} Network-level AAQMN records may be shown alongside records that explicitly mention this station.`;commonPanel(`${s.name} AAQMN station`,'EMA Ambient Air Quality Monitoring Network',note,rows,s.name,[]);const panel=$('placePanel');if(panel&&s.source_url)panel.insertAdjacentHTML('beforeend',`<p><a href="${esc(s.source_url)}" target="_blank" rel="noopener">Open coordinate/source evidence ↗</a></p>`);}
  function showIMALayerSummary(cfg,count){const rows=layerRelatedRecords(cfg,{});$('placePanel').innerHTML=`<h3>${esc(cfg.name)}</h3><p class="empty-state">Institute of Marine Affairs Marine Data Hub · ${count} mapped feature${count===1?'':'s'} returned by the selected live layer.</p><div class="legal-map-note">Only one IMA layer is shown at a time. Layer features are discovery context and do not constitute an EMA determination or project/regulatory boundary.</div><div class="record-list">${recordList(rows)||'<p class="empty-state">No related knowledge records match the current source filter.</p>'}</div><p><a href="${esc(cfg.service_url)}" target="_blank" rel="noopener">Open IMA REST service ↗</a> · <a href="${IMA_LIBRARY}" target="_blank" rel="noopener">IMA Library ↗</a></p>`;bindListButtons();}
  function showIMAFeature(cfg,props){const rows=layerRelatedRecords(cfg,props);const label=featureLabel(props)||'Selected feature';const safeProps=Object.entries(props||{}).filter(([,v])=>v!==null&&v!==''&&typeof v!=='object').slice(0,8);$('placePanel').innerHTML=`<h3>${esc(label)}</h3><p class="empty-state">${esc(cfg.name)} · Institute of Marine Affairs</p>${safeProps.length?`<div class="feature-attrs">${safeProps.map(([k,v])=>`<div><strong>${esc(k)}</strong><span>${esc(v)}</span></div>`).join('')}</div>`:''}<div class="legal-map-note">Feature attributes are displayed as supplied by the IMA service. TRACE does not reinterpret their regulatory, environmental or legal meaning.</div><div class="record-list">${recordList(rows)||'<p class="empty-state">No related knowledge records match the current filters.</p>'}</div><p><a href="${esc(cfg.service_url)}" target="_blank" rel="noopener">Open IMA REST service ↗</a></p>`;bindListButtons();}

  function renderCalendar(){const rows=state.observances.filter(o=>state.calendarTopic==='all'||o.topic===state.calendarTopic).sort((a,b)=>a.month-b.month||a.day-b.day);$('observanceCount').textContent=state.observances.length;const months=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];$('calendarGrid').innerHTML=rows.map(o=>`<article class="observance-card"><div class="date-box"><strong>${o.day}</strong><span>${months[o.month-1]}</span></div><div><h3>${esc(o.name)}</h3><p>${esc(o.topic)}</p><div class="custodian"><strong>${esc(o.custodian_type)}</strong><br>${esc(o.custodian)}</div><p>${esc(o.note)}</p><div class="observance-links"><a href="${esc(o.ema_url)}" target="_blank" rel="noopener">EMA context ↗</a>${o.custodian_url?`<a href="${esc(o.custodian_url)}" target="_blank" rel="noopener">International reference ↗</a>`:''}</div></div></article>`).join('');}
})();
